Inside the folder myprojects is a XAMPP project the "queueing".

NOTE: You can run this web app if you install the XAMPP 8.1.10 for windows first.
Then configure inside the database in phpmyadmin.

Many thanks
& GOD Bless!
